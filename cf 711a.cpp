#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n;

    string s1;

    cin>>n;

    vector<string>s;

    do
    {
        getline(cin,s1);

        s.push_back(s1);
    }
    while(n--);

    for(int i=0; i<s.size(); i++)
    {
       for(int j=0; j<s.size(); j++)
       {
           if()
       }
    }
}
